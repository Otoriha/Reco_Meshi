<%= @resource.email %>様

パスワードリセットのご依頼をいただきました。

以下のリンクをクリックして、新しいパスワードを設定してください：

<%= "#{root_url}password/reset?reset_password_token=#{@token}" %>

このリンクは6時間有効です。

パスワードのリセットをご依頼されなかった場合は、このメールを無視してください。
パスワードは変更されません。

レコめしチーム
