<%= @email %>様

レコめしへようこそ!

以下のリンクをクリックして、メールアドレスを確認してください：

<%= mailer.build_email_url('/settings/email-confirmation', 'confirmation_token', @token) %>

このリンクは24時間有効です。

心当たりがない場合は、このメールを無視してください。

レコめしチーム
